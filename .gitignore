# ---- Secrets ----------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# ---- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.venv/
venv/
env/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
.tox/
.hypothesis/

# ---- Node / web -------------------------------------------------------------
node_modules/
.npm/
dist/
dist-ssr/
.vite/
*.local
.eslintcache
coverage/
playwright-report/
test-results/
blob-report/
.playwright/

# NOTE: generated API types are deliberately NOT ignored. They are committed so a
# backend contract change shows up as a reviewable diff in the client, and so CI can
# type-check the web app without standing up the API first. Regenerate with
# `npm run generate:api`; never hand-edit.

# ---- Mobile / Expo ----------------------------------------------------------
.expo/
.expo-shared/
*.jks
*.p8
*.p12
*.mobileprovision
*.orig.*
web-build/

# ---- Infrastructure ---------------------------------------------------------
.docker/
docker-compose.override.yml
caddy_data/
caddy_config/
pgdata/

# ---- Application artifacts --------------------------------------------------
# Eval reports are run output; EVALS.md at the repo root IS committed.
backend/evals/reports/
*.sqlite3
*.db
tmp/
scratch/

# ---- Editors / OS -----------------------------------------------------------
.vscode/*
!.vscode/extensions.json
!.vscode/settings.shared.json
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ---- Agent tooling ----------------------------------------------------------
# .claude/settings.json IS committed: it records the project's toolkit and
# contains no paths or credentials. Machine-specific overrides are not.
.claude/settings.local.json
.remember/

# TypeScript incremental build cache (machine-local output, not source).
*.tsbuildinfo
